name: party_weather
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  equatable: ^2.0.5
  faker: ^2.1.0
  flutter:
    sdk: flutter
  uno: ^1.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutterando_analysis: ^0.0.2

flutter:
  uses-material-design: true
